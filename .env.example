# Copy to .env.local and fill in (Supabase → Project Settings → API).
# Both keys are browser-safe (RLS protects data); never put the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
